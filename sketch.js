function setup() {
  createCanvas(windowWidth, windowHeight);
  background(50, 150, 200);
  fill(255);
  textAlign(CENTER, CENTER);
  textSize(32);
  text("🐣 P5 IS WORKING!", width / 2, height / 2);
  console.log("✅ p5.js loaded and canvas created!");
}

function draw() {}
